---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="🤖 markoegea 👾" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/jpg" href="/profile.jpg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
	@font-face {
		font-family: 'Kanit-Light';
		src: url('/fonts/Kanit-Light.ttf'); 
		font-weight: normal;
		font-style: normal;
		font-display: swap;
	}

	html {
		font-family: 'Kanit-Light';
		background: #121212;
		letter-spacing: -0.025rem;
	}

	body, figure {
		margin:0;
		padding: 0;
	}

	a {
		text-decoration: none;
	}

	a:hover{
        text-decoration: underline;
    }

	ul {
		list-style: none;
		margin: 0;
		padding: 0;
	}

	*, *::before, *::after {
		box-sizing: border-box;
	}

	h1, h2, h3, h4 {
		color: #d6d6d6;
		margin: 0;
		font-family: 'Kanit-Light';
	}

	p {
		color: #999;
		font-size: 0.95rem;
		line-height: 1.5;
		margin: 0;
		text-wrap: pretty;
	}

	@media print {
		.no-print {
			display: none !important;
		}
	}

	.print {
		display: none !important;
	}

	@media print {
		.print {
			display: block !important;
		}
	}
</style>
